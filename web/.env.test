# Public Supabase connection for the auth-flow tests (committed on purpose).
# The publishable/anon key is designed to ship in the browser and is protected
# by row-level security — the same value is already in the deployed bundle.
# The SECRET service-role key is never committed; supply it via the environment
# (SUPABASE_SERVICE_ROLE_KEY) to also run the end-to-end email-OTP tests.
NEXT_PUBLIC_SUPABASE_URL=https://dowvjokcncfombjjozff.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_DKitA0hsKeZutx5YKK4Mvw_7JeOIzHo
